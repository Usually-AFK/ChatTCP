<UserControl x:Class="WpfChatClient.Views.ConnectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:WpfChatClient.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:ConnectViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="Transparent">
        <Border Style="{StaticResource BrightGlassCard}" 
                VerticalAlignment="Center" HorizontalAlignment="Center" 
                Width="350"
                Padding="30">
            <StackPanel>
                <TextBlock Text="Welcome back" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrightTextPrimary}" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="Connect to the global chat room" FontSize="13" Foreground="{StaticResource BrightTextSecondary}" HorizontalAlignment="Center" Margin="0,0,0,25"/>
                
                <TextBlock Text="Server IP" Foreground="{StaticResource BrightTextSecondary}" FontSize="11" FontWeight="Bold" Margin="2,0,0,5"/>
                <ui:TextBox Text="{Binding Ip, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource BrightGlassTextBox}"
                         PlaceholderText="Host LAN IP, e.g. 192.168.2.18"
                         Margin="0,0,0,6"/>
                <TextBlock Text="Same PC: 127.0.0.1. Classmates: enter the host Wi-Fi IPv4 address from ipconfig."
                           Foreground="{StaticResource BrightTextSecondary}"
                           FontSize="11"
                           TextWrapping="Wrap"
                           Margin="2,0,0,15"/>
                
                <TextBlock Text="Username" Foreground="{StaticResource BrightTextSecondary}" FontSize="11" FontWeight="Bold" Margin="2,0,0,5"/>
                <ui:TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource BrightGlassTextBox}"
                         PlaceholderText="Enter your name"
                         Margin="0,0,0,10"/>
                
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF6B6B" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <ui:Button Content="Login" 
                        Command="{Binding ConnectCommand}" 
                        Style="{StaticResource ModernButton}"
                        Height="45"
                        HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
